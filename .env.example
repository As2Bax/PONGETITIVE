# Port the room server listens on.
PORT=8080

# Comma-separated list of origins allowed to open a WebSocket connection.
# Leave unset to allow any origin (development default).
# PONG_ALLOWED_ORIGINS=https://example.com,https://www.example.com
